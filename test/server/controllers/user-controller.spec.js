var expect     = require('chai').expect;
var controller = require('../../../server/controllers/user-controller');

describe('user controller', () => {
  //TODO: remove
  it('mocha check', done => {
    done();
  });
});
